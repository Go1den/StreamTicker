[WindowSettings]

# This is the X coordinate where the message will begin to be displayed in the window. Increase this to move messages to the right. Decrease to move them to the left.
MESSAGE_X_POS = 60

# This is the X coordinate where the CENTER of your images should be displayed in the window. Increase this to move images to the right. Decrease to move them to the left.
IMAGE_X_POS = 22

# Width of the window in pixels. For the program to work properly, it is highly recommended to use a multiple of 4.
WINDOW_WIDTH = 400

# Height of the window in pixels. For the program to work properly, it is highly recommended to use a multiple of 4.
WINDOW_HEIGHT = 44

# Default window background color. Plain color words are accepted, or if you know hex, you can also type this in the format #123456 (include the # sign)
WINDOW_BG_COLOR = black

# This is the location of the background image. If no image is provided or the filepath is bad, the background color above will be used.
WINDOW_BG_IMAGE = imagefiles/background.png

# This is the X coordinate where the CENTER of your background image should be displayed in the window. Increase this to move it to the right. Decrease to move it to the left.
BACKGROUND_X_POS = 202

# This is the Y coordinate where the CENTER of your background image should be displayed in the window. Increase this to move it down. Decrease to move it up.
BACKGROUND_Y_POS = 24

# This is the delay in seconds between each move action. This simulates fluid movement of an object off the screen.
# Recommendation is not to mess with this, but you can try it for fun. Default value is .004
MOVE_ALL_ON_LINE_DELAY = .004